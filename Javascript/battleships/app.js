var shipMid = function() {
  min = Math.ceil(1);
  max = Math.floor(6);
  return Math.floor(Math.random() * (max - min)) + min;
}
var shipBow = shipMid - 1;
var shipStern = shipMid + 1;

while (isShipAlive) {

}
